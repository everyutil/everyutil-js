/**
 * Gets the average color of an image or canvas (stub, needs real implementation).
 *
 * Example: averageColor(image)
 *
 * @author @dailker
 * @param {HTMLImageElement|HTMLCanvasElement} image - Image or canvas.
 * @returns {string} Average color in hex.
 */
export function averageColor(image: any): string {
  // Stub: real implementation would use canvas pixel data
  return '#000000';
}
