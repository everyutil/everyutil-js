import { boolClusterCount } from '../../src/logic/boolClusterCount';

describe('boolClusterCount', () => {
  it('should be a function', () => {
    expect(typeof boolClusterCount).toBe('function');
  });
});
