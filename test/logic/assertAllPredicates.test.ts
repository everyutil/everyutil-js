import { assertAllPredicates } from '../../src/logic/assertAllPredicates';

describe('assertAllPredicates', () => {
  it('should be a function', () => {
    expect(typeof assertAllPredicates).toBe('function');
  });
});
