import { assertPredicate } from '../../src/logic/assertPredicate';

describe('assertPredicate', () => {
  it('should be a function', () => {
    expect(typeof assertPredicate).toBe('function');
  });
});
