import { booleanByEntropy } from '../../src/logic/booleanByEntropy';

describe('booleanByEntropy', () => {
  it('should be a function', () => {
    expect(typeof booleanByEntropy).toBe('function');
  });
});
