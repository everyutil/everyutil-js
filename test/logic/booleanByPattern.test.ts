import { booleanByPattern } from '../../src/logic/booleanByPattern';

describe('booleanByPattern', () => {
  it('should be a function', () => {
    expect(typeof booleanByPattern).toBe('function');
  });
});
