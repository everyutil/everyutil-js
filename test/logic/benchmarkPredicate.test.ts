import { benchmarkPredicate } from '../../src/logic/benchmarkPredicate';

describe('benchmarkPredicate', () => {
  it('should be a function', () => {
    expect(typeof benchmarkPredicate).toBe('function');
  });
});
