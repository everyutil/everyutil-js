import { countWeekdaysBetween } from '../../src/date/countWeekdaysBetween';

describe('countWeekdaysBetween', () => {
  it('should be a function', () => {
    expect(typeof countWeekdaysBetween).toBe('function');
  });
});
