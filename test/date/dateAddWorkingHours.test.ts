import { dateAddWorkingHours } from '../../src/date/dateAddWorkingHours';

describe('dateAddWorkingHours', () => {
  it('should be a function', () => {
    expect(typeof dateAddWorkingHours).toBe('function');
  });
});
