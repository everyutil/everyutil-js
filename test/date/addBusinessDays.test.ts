import { addBusinessDays } from '../../src/date/addBusinessDays';

describe('addBusinessDays', () => {
  it('should be a function', () => {
    expect(typeof addBusinessDays).toBe('function');
  });
});
